---
// Dark mode icon component with consistent colors
interface Props {
    size?: number;
    class?: string;
}

const { size = 40, class: className = "" } = Astro.props;
---

<div class={`text-[#74becc] ${className}`} style={`font-size: ${size}px;`}>
    <slot />
</div>
